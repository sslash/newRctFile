import React, {Component, PropTypes} from 'react';

export default class __REACT_CLASS__ extends Component {

    static propTypes = {
    }

    static defaultProps = {
    }

    constructor (props) {
        super(props);
        this.state = {};
    }

    render () {
        return (
            <div></div>
        );
    }
}
